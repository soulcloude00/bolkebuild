const panels=[...document.querySelectorAll('.panel')],steps=[...document.querySelectorAll('.step')];
let selectedLang='hi-IN',approved=false;
const show=id=>{panels.forEach(p=>p.classList.toggle('active',p.id===id));steps.forEach(s=>s.classList.toggle('active',s.dataset.go===id));location.hash=id;scrollTo(0,0)};
document.querySelectorAll('[data-go]').forEach(b=>b.addEventListener('click',()=>{const target=b.dataset.go;if(target==='understand'&&!document.querySelector('#ruleText').value.trim())return;show(target)}));
document.querySelectorAll('.lang').forEach(b=>b.onclick=()=>{document.querySelectorAll('.lang').forEach(x=>x.classList.remove('active'));b.classList.add('active');selectedLang=b.dataset.lang});
document.querySelectorAll('[data-sample]').forEach(b=>b.onclick=()=>document.querySelector('#ruleText').value=b.dataset.sample);
document.querySelector('#buildRule').onclick=()=>{let t=document.querySelector('#ruleText').value.trim();if(!t){document.querySelector('.recorder').classList.add('shake');setTimeout(()=>document.querySelector('.recorder').classList.remove('shake'),600);return}document.querySelector('#heard').textContent='“'+t+'”';const lower=t.toLowerCase();if(lower.includes('supplier')||lower.includes('पूछे')){trigger.textContent='Before any supplier message is prepared';action.textContent='Pause and ask the manager';recipient.textContent='Manager approval queue'}else if(lower.includes('शाम')||lower.includes('evening')){trigger.textContent='Every evening at shop close';action.textContent='Check the demo inventory';recipient.textContent='You, in Telegram'}else{trigger.textContent='Inventory for any item drops below 5';action.textContent='Prepare a low-stock alert';recipient.textContent='You, in Telegram'}show('understand')};
const mic=document.querySelector('#mic'),status=document.querySelector('#micStatus');let recognition;
mic.onclick=()=>{const SR=window.SpeechRecognition||window.webkitSpeechRecognition;if(!SR){status.textContent='Live speech is not supported in this browser. Type or choose a sample.';return}recognition=new SR();recognition.lang=selectedLang;recognition.interimResults=true;mic.classList.add('listening');mic.querySelector('b').textContent='Listening…';status.textContent='Speak naturally. Tap again to stop.';recognition.onresult=e=>{ruleText.value=[...e.results].map(r=>r[0].transcript).join('')};recognition.onerror=e=>status.textContent='Mic could not start: '+e.error+'. You can still type the rule.';recognition.onend=()=>{mic.classList.remove('listening');mic.querySelector('b').textContent='Speak now';status.textContent='Transcript ready. Edit anything it misheard.'};recognition.start()};
document.querySelector('#deny').onclick=()=>{deniedNote.textContent='Nothing ran. The rule is saved as a draft until you approve.'};
document.querySelector('#approve').onclick=()=>{approved=true;show('result')};document.querySelector('#again').onclick=()=>{approved=false;deniedNote.textContent='';show('listen')};
if(location.hash&&document.querySelector(location.hash))show(location.hash.slice(1));
